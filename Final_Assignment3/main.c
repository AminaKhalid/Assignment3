#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>
#include "CrossFireOperations.h"

int main(void)
{
	int boardSize
	
	boardSize = 7;
	
	int getBoardSize();
	//number of players and their types
	playerData();
	
	//Creates the board
	createBoard(BoardSize, &upLeft, &upRight, &downLeft, &downRight);
	
	//Fill each player into a random position on the board
	PlayersonBoard(size);
	
	void choice();
	
	return 0;
}